name: Frontend Unit Tests (moved)

on:
  workflow_call: {}

jobs:
  moved-note:
    runs-on: ubuntu-latest
    steps:
      - name: Inform
        run: |
          echo "This workflow has been moved to: spec-kit_demo/java/forget-pass/.github/workflows/frontend-tests.yml"
          echo "Please update CI references to the new location."
