name: Root Tests

on:
  push:
    branches:
      - 'feature/**'
  pull_request:
    branches:
      - main

jobs:
  call-frontend-tests:
    uses: ./.github/workflows/frontend-tests.yml
    with: {}
  call-backend-tests:
    uses: ./.github/workflows/backend-tests.yml
    with: {}
